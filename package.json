{
  "name": "lazarus-mesh",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first deterministic recovery marketplace simulation with Rain and Monad adapters.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --test"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "license": "MIT",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
